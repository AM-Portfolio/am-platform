# Preprod Environment — am-identity
environment: preprod
replicaCount: 1

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 1m
    memory: 64Mi

vault:
  enabled: true
  secretPaths:
    otel:
      path: "apps/data/preprod/infra/observability"
    identity:
      path: "apps/data/preprod/services/am-identity"


env:
  APP_ENV: preprod
  LOG_LEVEL: INFO
  IDENTITY_VERIFY_SSL: "false"

# Preprod app host only — am.asrax.in is owned by prod (no Host collision)
ingress:
  enabled: true
  className: "traefik"
  annotations:
    kubernetes.io/ingress.class: "traefik"
    traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
    traefik.ingress.kubernetes.io/router.middlewares: >-
      am-apps-preprod-global-cors@kubernetescrd,
      am-apps-preprod-strip-prefix-apps@kubernetescrd
  hosts:
    - host: am-preprod.asrax.in
      paths:
        - path: /identity
          pathType: Prefix

