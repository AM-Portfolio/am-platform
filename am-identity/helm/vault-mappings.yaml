vault:
  secretPaths:
    identity:
      mappings:
        KEYCLOAK_URL: "KEYCLOAK_URL"
        KEYCLOAK_ADMIN_USER: "KEYCLOAK_ADMIN_USER"
        KEYCLOAK_ADMIN_PASSWORD: "KEYCLOAK_ADMIN_PASSWORD"
        KEYCLOAK_REALM: "KEYCLOAK_REALM"
        OIDC_DISCOVERY_URL: "OIDC_DISCOVERY_URL"
        OIDC_ISSUER: "OIDC_ISSUER"
        OIDC_JWKS_URL: "OIDC_JWKS_URL"
        OIDC_TOKEN_URL: "OIDC_TOKEN_URL"
        GOOGLE_CLIENT_ID: "GOOGLE_CLIENT_ID"
        GOOGLE_CLIENT_SECRET: "GOOGLE_CLIENT_SECRET"
        AM_IDENTITY_CLIENT_ID: "AM_IDENTITY_CLIENT_ID"
        AM_IDENTITY_CLIENT_SECRET: "AM_IDENTITY_CLIENT_SECRET"
        ALLOWED_GOOGLE_REDIRECT_URIS: "ALLOWED_GOOGLE_REDIRECT_URIS"
        JWT_SECRET: "JWT_SECRET"
        AUTH_UI_BASE_URL: "AUTH_UI_BASE_URL"
        AUTH_EMAIL_TOKEN_SECRET: "AUTH_EMAIL_TOKEN_SECRET"
        AUTH_EMAIL_TOKEN_TTL_SECONDS: "AUTH_EMAIL_TOKEN_TTL_SECONDS"
        SMTP_HOST: "SMTP_HOST"
        SMTP_PORT: "SMTP_PORT"
        SMTP_USER: "SMTP_USER"
        SMTP_PASSWORD: "SMTP_PASSWORD"
        SMTP_FROM: "SMTP_FROM"
        SMTP_FROM_DISPLAY_NAME: "SMTP_FROM_DISPLAY_NAME"
        SMTP_SSL: "SMTP_SSL"
        SMTP_STARTTLS: "SMTP_STARTTLS"

    otel:
      mappings:
        TRACING_SAMPLING_PROBABILITY: "TRACING_SAMPLING_PROBABILITY"
