name: am-identity
release: am-identity
image: ghcr.io/am-portfolio/am-identity
runtime: python
language: python
dockerfile: Dockerfile
# Dockerfile COPYs libraries/ + am-identity/ — build from am-platform root
context: ..
github:
  repo: AM-Portfolio/am-platform
  deploy_workflow: central-build-publish.yml
  service: am-identity
  ref: main
deploy:
  via: helm
  rebuild: on-change
