name: AM Notification Publish

on:
  push:
    branches: ["main", "develop", "test/**", "feature/**", "hotfix/**", "fix/**"]
    paths:
      - "am-notification/**"
      - "libraries/**"
      - "requirements.txt"
      - "requirements-dev.txt"
      - ".dockerignore"
  pull_request:
    branches: ["main", "develop", "test/**", "feature/**", "hotfix/**", "fix/**"]
    paths:
      - "am-notification/**"
      - "libraries/**"
      - "requirements.txt"
      - "requirements-dev.txt"
      - ".dockerignore"
  workflow_dispatch:

jobs:
  publish:
    name: Publish AM Notification
    permissions:
      contents: read
      packages: write
    uses: AM-Portfolio/am-pipelines/.github/workflows/central-build-publish.yml@main
    with:
      language: "python"
      working_directory: "am-notification"
      image_name: "am-notification"
      build_context: ".."
      preprod_namespace: "am-apps-preprod"
      prod_namespace: "am-apps-prod"
      deploy_dev: false
      deploy_prod: true
    secrets: inherit
