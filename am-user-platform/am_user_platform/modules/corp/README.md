# Corp module

**Status:** Not implemented in v1.

Reserved for employee settings with `org_id` (am-corps / enterprise).

| Planned later | Notes |
|---------------|-------|
| Schema | `corp` (Postgres) |
| Key column | `org_id` (required when this module ships) |

Do **not** import this module from `modules/ai` or `modules/preferences`.
