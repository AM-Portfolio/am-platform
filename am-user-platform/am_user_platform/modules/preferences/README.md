# Preferences module

**Status:** Not implemented in v1.

Reserved for user dashboard layout, UI flags, and similar preferences.

| Planned later | Notes |
|---------------|-------|
| Schema | `prefs` (Postgres) |
| Ownership | User JWT `sub` only — no org |

Do **not** import this module from `modules/ai` or `modules/corp`.
