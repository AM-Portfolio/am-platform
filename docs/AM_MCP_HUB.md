# AM MCP Hub

See service README: [am-mcp-hub/README.md](../am-mcp-hub/README.md).

Production MCP product: Postgres catalog, Vault secret refs, aggregated `/mcp`, thin `/admin/` ops page.
**UI:** official MCP Inspector (`am ai inspect`). Local stack: `docker compose up` in `am-mcp-hub` (hub :8130 + google-workspace :8000).
